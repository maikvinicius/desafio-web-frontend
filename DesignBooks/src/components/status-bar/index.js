import React from 'react';

import {
    Base,
    StatusBarStyle
} from './styles';

const StatusBar = () => {
    return (
        <Base>
            <StatusBarStyle />
        </Base>
    );
}

export default StatusBar;